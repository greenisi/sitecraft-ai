import { VisualStudioDemo } from '@/components/media/visual-studio-demo';

export const metadata = {
  title: 'AI Visual Studio Demo | Sitecraft',
  description: 'A local, UI-only preview of the Sitecraft AI Visual Studio journey.',
};

export default function VisualStudioDemoPage() {
  return <VisualStudioDemo />;
}
